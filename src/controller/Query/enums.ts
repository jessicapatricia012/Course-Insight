export enum SField {
	uuid = "uuid",
	id = "id",
	title = "title",
	instructor = "instructor",
	dept = "dept",
}

export enum MField {
	avg = "avg",
	pass = "pass",
	fail = "fail",
	audit = "audit",
	year = "year",
}

export enum MComparator {
	LT = "LT",
	GT = "GT",
	EQ = "EQ",
}

export enum Logic {
	AND = "AND",
	OR = "OR",
}
